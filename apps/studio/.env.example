# Base URL for the signal-platform FastAPI backend.
# Production (same-origin behind Caddy on signal.cimcha.com): leave unset — the
# client defaults to "/api", which Caddy proxies to the api service.
# Local dev against the live backend:
#   VITE_API_BASE=https://signal.cimcha.com/api
VITE_API_BASE=/api
