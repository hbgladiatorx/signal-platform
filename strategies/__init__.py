"""User-defined strategies.

This directory is scanned by packages.strategy.registry.discover_strategies()
at runtime. Drop a .py file here defining a Strategy subclass to make it
available to the backtest worker and UI.

Files beginning with underscore are skipped.
"""
